plugin: microsoft.ad.ldap

search_base: OU=NSH,DC=nsh,DC=lab
groups:
  NSHLAB:
    members_filter: "(&(objectClass=computer)(memberOf=cn=Servers,ou=NSH,dc=nsh,dc=lab))"
    compose:
      ansible_host: hostnameIP

compose:
  hostnameIP: "{ipHostNumber}"
